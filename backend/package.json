{
  "name": "mini-ops-erp-backend",
  "version": "1.0.0",
  "description": "Mini Operations ERP - Backend API",
  "main": "src/index.js",
  "scripts": {
    "dev": "node src/index.js",
    "start": "node src/index.js",
    "seed": "node src/seed.js",
    "pretest": "DOTENV_CONFIG_PATH=.env.test npx prisma db push --skip-generate",
    "test": "DOTENV_CONFIG_PATH=.env.test jest --runInBand",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "prisma": "^5.19.1",
    "supertest": "^7.0.0"
  }
}
