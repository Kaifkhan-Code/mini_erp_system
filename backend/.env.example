# Copy this file to .env and adjust values.
# Default project database is PostgreSQL (Neon).
# Replace the example connection string below with your Neon project URL.

DATABASE_URL="postgresql://neondb_owner:your_password@ep-curly-breeze-123456.us-east-1.aws.neon.tech/neondb?sslmode=require"
JWT_SECRET="replace-this-with-a-long-random-secret"
JWT_EXPIRES_IN="8h"
PORT=4000
